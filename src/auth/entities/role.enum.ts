export enum Role{
    USER = 'User',
    ADMIN = 'Admin'
}

export enum Permissions{
    CREATE = 'Create',
    UPDATE = 'Update',
    DELETE = 'Delete',
    READ = 'Read'

}
// import { RolesService } from "src/roles/roles.service";
